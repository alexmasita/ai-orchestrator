# Replace this placeholder with your actual Vast API key before launching.
vast_api_key: "bfc42d92b18f1ac049575940992fa70bf492767b5822f4d9556239a63a913751"
vast_api_url: "https://console.vast.ai/api/v0"

gpu:
  # Minimum required GPU VRAM in GB.
  min_vram_gb: 24
  # Preferred GPU models in priority order.
  preferred_models:
    - RTX_4090
    - RTX_A6000
    - A100

min_inet_down_mbps: 100
min_inet_up_mbps: 100

min_reliability: 0.98
verified_only: true

max_dph: 1.0
allow_interruptible: true
limit: 5

idle_timeout_seconds: 1800
snapshot_version: "v1"

# Required when "whisper" is included in --models.
whisper_vram_gb: 8
# Required when "whisper" is included in --models.
whisper_disk_gb: 10
